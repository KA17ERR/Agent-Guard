fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
SQLAlchemy==2.0.35
python-dotenv==1.0.1
httpx==0.27.2
pytest==8.3.3
openai==1.51.0
google-generativeai==0.8.3
# Postgres driver — only used if DATABASE_URL points at postgres:// (see
# DEPLOYMENT.md). Harmless to have installed even when running SQLite
# locally; database.py picks the right behavior based on DATABASE_URL.
psycopg2-binary==2.9.10
