[pytest]
# SQLAlchemy/Pydantic classes like TestRun, TestRunRead are domain models,
# not pytest test classes — they trigger PytestCollectionWarning purely
# because their name starts with "Test". Silencing just that warning class
# keeps real warnings (deprecations, etc.) visible.
filterwarnings =
    ignore::pytest.PytestCollectionWarning
